# Signing keys
MbtBizizAndroid/private_key.pepk
MbtBizizAndroid/signKey

# Release credentials (contains passwords — never commit)
.release-credentials

# Build artifacts
release_artifacts/
release_artifacts_bordro/
