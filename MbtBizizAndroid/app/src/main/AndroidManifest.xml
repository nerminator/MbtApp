<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daimler.biziz.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera2" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher2"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/SplashTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.about.AboutActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.main.main.MainActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/LoginTheme" />
        <activity android:name=".ui.main.orchestra.flexibleWorkingInfo.FlexibleWorkingActivity" />
        <activity android:name=".ui.main.orchestra.flexibleWorkingInfo.FlexibleWorkingDetailActivity" />
        <activity android:name=".ui.newsDetail.NewsDetailActivity" />
        <activity android:name=".ui.birthday.BirthdayActivity" />
        <activity android:name=".ui.foodMenu.FoodMenuActivity" />
        <activity android:name=".ui.main.orchestra.schedule.WorkingScheduleActivity" />
        <activity android:name=".ui.news.NewsActivity" />
        <activity android:name=".ui.residential.ResidentialActivity" />
        <activity android:name=".ui.transportation.TransportationActivity" />
        <activity android:name=".ui.transportation.search.SearchShuttleActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />

        <receiver
            android:name=".receiver.LogOutReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.daimler.biziz.intent.LOG_OUT" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_api_key" />

        <activity android:name=".ui.residential.SearchRoomActivity"></activity>

        <service android:name=".firebase.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service android:name=".firebase.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_ic_notification" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimaryDark" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
    </application>

</manifest>
